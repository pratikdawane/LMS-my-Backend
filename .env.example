# ============================================
# BACKEND ENVIRONMENT CONFIGURATION
# ============================================
# For local development, copy this to .env and fill in your values
# For production (Render), set these variables in Render dashboard
# ============================================

# Environment (development for local, production on Render)
NODE_ENV=development

# Database - MongoDB connection string
MONGO_URI=mongodb://localhost:27017/linkcode-lms

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=15m
ACCESS_TOKEN_MAX_AGE_MS=900000

# Server Configuration
PORT=4000

# CORS Configuration
# Comma-separated list of allowed origins (NO SPACES after commas)
# Local Development Example:
# CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:3000
# Production Example:
# CORS_ORIGINS=https://lms-frontend-vjck.onrender.com
CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:3000  

# ========================================
# EMAIL CONFIGURATION - SENDGRID (For Render Free Tier)
# ========================================
# Get API Key from: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=SG.your_sendgrid_api_key_here

# From Email - Must be verified in SendGrid
# Get from: SendGrid Dashboard  Settings  Sender Authentication  Verify a New Sender
# This is the email address that will appear as the sender
# Example: noreply@yourdomain.com or your_email@gmail.com
SENDGRID_FROM_EMAIL=noreply@yourdomain.com
# ========================================
# EMAIL CONFIGURATION - SMTP (For Vultr/AWS/Other VPS)
# ========================================
# Uncomment and configure these when you move to Vultr/AWS
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# AWS S3 Configuration (Optional - for file uploads)
AWS_REGION=ap-south-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_BUCKET_NAME=

# Razorpay Configuration (Optional - for payments)
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=
